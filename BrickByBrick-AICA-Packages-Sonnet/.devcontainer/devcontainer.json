{
  "name": "brickbybrick-sonnet",
  "containerUser": "ros2",
  "initializeCommand": [
    "docker",
    "build",
    "--file=aica-package.toml",
    "--target=development",
    "--build-arg=config.signing.enabled=false",
    "--tag=aica-technology/brickbybrick-sonnet:development",
    "."
  ],
  "image": "aica-technology/brickbybrick-sonnet:development",
  "postStartCommand": "rm -r /home/ros2/ws/src/brickbybrick_sonnet && ln -s ${containerWorkspaceFolder}/source/brickbybrick_sonnet /home/ros2/ws/src/brickbybrick_sonnet",
  "postCreateCommand": "/usr/local/bin/generate_compile_commands -p brickbybrick_sonnet -t 60 -o ${containerWorkspaceFolder} --cmake-args BUILD_TESTING=ON",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools-extension-pack",
        "llvm-vs-code-extensions.vscode-clangd",
        "xaver.clang-format",
        "charliermarsh.ruff"
      ]
    }
  }
}